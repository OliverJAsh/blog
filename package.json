{
  "private": true,
  "scripts": {
    "compile": "cd src && babel *.js --out-dir ../target && babel views --out-dir ../target/views && babel posts --out-dir ../target/posts",
    "start": "node target/ >> log.txt",
    "build": "gulp build",
    "clean": "rm -rf target",
    "lint": "eslint ."
  },
  "dependencies": {
    "babel-cli": "^6.2.0",
    "babel-preset-es2015": "^6.1.18",
    "compression": "^1.5.2",
    "dateformat": "https://registry.npmjs.org/dateformat/-/dateformat-1.0.12.tgz",
    "eslint": "^1.10.3",
    "eslint-plugin-html": "^1.1.0",
    "express": "^4.13.3",
    "gulp": "^3.9.0",
    "lodash": "^3.10.1",
    "promised-io": "^0.3.5",
    "slug": "^0.9.1",
    "sorted-object": "^1.0.0",
    "vdom-to-html": "^2.1.1",
    "virtual-dom": "^2.1.1"
  }
}
