{
  "private": true,
  "scripts": {
    "start": "npm run compile && npm run runServer",
    "compile": "npm run copyImages && npm run babelCompile",
    "babelCompile": "cd src && babel *.js --out-dir ../target && babel views --out-dir ../target/views && babel posts --out-dir ../target/posts",
    "runServer": "node target/index.js >> log.txt",
    "copyImages": "cp -r ./src/public/images/* ./target/public/images",
    "lint": "eslint ."
  },
  "dependencies": {
    "babel-cli": "^6.2.0",
    "babel-preset-es2015": "^6.1.18",
    "compression": "^1.5.2",
    "dateformat": "https://registry.npmjs.org/dateformat/-/dateformat-1.0.12.tgz",
    "denodeify": "^1.2.1",
    "eslint": "^1.10.3",
    "express": "^4.13.3",
    "lodash": "^3.10.1",
    "slug": "^0.9.1",
    "vdom-to-html": "^2.1.1",
    "virtual-dom": "^2.1.1"
  }
}
